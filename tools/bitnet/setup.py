from setuptools import setup, find_packages

setup(
    name='bitnetsrc',
    version='0.1',
    description='Fused BitNet Components',
    author='Xun Wu',
    packages=find_packages(),  # 自动查找所有含 __init__.py 的包
    install_requires=[],
)
